# Default env for local quick dev
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=rootpassword

BACKEND_PORT=3000
NODE_ENV=development
HOST=0.0.0.0
MONGO_URI=mongodb://root:rootpassword@mongodb:27017/hvn?authSource=admin
JWT_SECRET=your_jwt_secret_key_here

